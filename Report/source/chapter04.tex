% !TeX spellcheck = en_US
\chapter{Experiments and Results}%
\label{ch:experiments}%

This chapter describes the experimental realization of the methods introduced in \cref{ch:methods}.
It begins with the hardware and simulation setup (\cref{sec:experimental_setup}), details the teacher and student policy implementations (\cref{sec:teacher_implementation,sec:student_implementation}), defines the evaluation protocol (\cref{sec:eval_protocol}), and presents results for both teacher training (\cref{sec:teacher_results}) and distillation (\cref{sec:distillation_results}).

%% =========================================================================
\section{Experimental Setup}%
\label{sec:experimental_setup}%

\subsection{Hardware Platform}%
\label{sec:hardware}%

The robotic platform consists of a \ac{FR3} 7-\acs{DoF} arm equipped with an Agile Hand, forming a 23-\acs{DoF} arm--hand system.

\paragraph{Franka Research~3.}
The \ac{FR3} is a torque-controlled collaborative robot arm with seven revolute joints and integrated joint torque sensors on every axis.
% PLACEHOLDER: Add FR3 payload, reach, repeatability specs. Cite Franka Emika datasheet.
The arm provides Cartesian impedance control capabilities, though in this work all joints are controlled via position targets with simulated PD controllers.
The factory torque limits are 87\,Nm for joints~1--4 and 12\,Nm for joints~5--7.

\paragraph{Agile Hand.}
The Agile Hand, manufactured by Agile Robots SE, is a 16-\acs{DoF} anthropomorphic robotic hand composed of five identical, modularly designed fingers with an aluminum open skeleton structure and injection-molded plastic shells.
The hand weighs 1.5\,kg and has 21~joints total, of which 16 are independently actuated (one coupled joint per finger).
Each finger has four joints: a metacarpophalangeal (MCP) pitch joint ($0\degree$ to $75\degree$), an MCP yaw joint ($\pm 15\degree$), a proximal interphalangeal (PIP) joint ($0\degree$ to $85\degree$), and a distal interphalangeal (DIP) joint ($0\degree$ to $85\degree$, coupled to PIP).
The opposing thumb adds a rotation \acs{DoF} with a range of $60\degree$.
The hand provides 10\,N active fingertip force, $360\degree$/s finger joint velocity, and EtherCAT-based real-time communication at 1\,kHz.
The thumb rotation joint operates at a significantly lower velocity ($\approx$12\,deg/s), which is a key constraint for sim-to-real transfer (see \cref{sec:sim2real_curriculum}).
Joint torque and position sensors are integrated in every actuated joint, enabling active compliant control.
The combined arm--hand system provides 23 actuated \acp{DoF}: 7~arm joints plus 16~hand joints.
\cref{fig:hardware_platform} shows the physical Agile Hand and the assembled system as rendered in Isaac Sim.

\paragraph{Sensor setup.}
% PLACEHOLDER: Describe Intel RealSense D405 — resolution, depth range, mounting position (wrist-mounted or external), frame rate. In simulation, stereo RGB rendering is used via Isaac Lab's TiledCamera at 320x240 resolution.

\begin{figure}[htb]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{setup/Agile_Hand_photo}
        \caption{The physical Agile Hand.}
        \label{fig:agile_hand_photo}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{setup/Setup_isaacsim}
        \caption{FR3 + Agile Hand in Isaac Sim.}
        \label{fig:setup_isaacsim}
    \end{subfigure}
    \caption{Hardware platform: (a)~the 16-\acs{DoF} Agile Hand manufactured by Agile Robots SE, and (b)~the combined FR3 arm--hand system as rendered in the Isaac Sim simulation environment.}
    \label{fig:hardware_platform}
\end{figure}

\subsection{Simulation Environment}%
\label{sec:sim_env}%

All training and distillation is performed in NVIDIA Isaac Lab~\cite{Mittal2023}, a GPU-accelerated simulation framework built on Isaac Sim and PhysX 5.

\paragraph{Direct environment API.}
Isaac Lab provides two paradigms for environment design: a \emph{manager-based} API and a \emph{direct} API (\texttt{DirectRLEnv}).
This work uses the direct API, which provides full transparency over the simulation loop: observations, rewards, resets, and domain randomization are implemented explicitly in Python rather than delegated to abstracted manager modules.
This is the same paradigm used in the published DextrAH-RGB implementation~\cite{Singh2025} and enables fine-grained control over reward computation, \ac{ADR} scheduling, and physics parameter manipulation at each time step.

\paragraph{Physics configuration.}
The simulation runs at 120\,Hz with a control frequency of 60\,Hz (decimation factor of~2).
\cref{tab:physics_config} summarizes the key PhysX solver parameters.

\begin{table}[htb]
    \centering
    \caption{PhysX simulation parameters.}
    \label{tab:physics_config}
    \begin{tabular}{lc}
        \toprule
        \textbf{Parameter} & \textbf{Value} \\
        \midrule
        Simulation frequency & 120\,Hz \\
        Control frequency & 60\,Hz \\
        Solver position iterations & 8 \\
        Solver velocity iterations & 0 \\
        Max depenetration velocity & 30.0\,m/s \\
        Bounce threshold velocity & 0.2\,m/s \\
        Friction offset threshold & 0.04\,m \\
        GPU max rigid patch count & $10 \times 2^{15}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\paragraph{Asset integration.}
The FR3 arm and Agile Hand are combined into a single articulated asset in \ac{USD} format.
Joint physics parameters (damping, stiffness, effort limits) are configured both in the USD file and via Isaac Lab's actuator API.
\cref{tab:actuator_params} lists the actuator parameters used for the final training configuration.

\begin{table}[htb]
    \centering
    \caption{Actuator parameters for the FR3 + Agile Hand system.}
    \label{tab:actuator_params}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Joint Group} & \textbf{Stiffness} & \textbf{Damping} & \textbf{Effort Limit (Nm)} \\
        \midrule
        Arm joints 1--4 & 200 & 40 & 100 \\
        Arm joints 5--7 & 200 & 40 & 50 \\
        Finger MCP pitch & 10.0 & 6.0 & 2.0 \\
        Finger MCP yaw & 10.0 & 6.0 & 2.0 \\
        Finger PIP & 10.0 & 6.0 & 2.0 \\
        Thumb rotation & 20.0 & 2.0 & 2.0 \\
        \bottomrule
    \end{tabular}
\end{table}

\paragraph{Contact physics.}
The fingertip material uses static friction~2.0, dynamic friction~1.5 at nominal conditions, with the global friction combine mode set to \texttt{max} so that the higher rubber fingertip friction dominates at the contact interface.
Object materials use static and dynamic friction of~1.0.
These values are subject to \ac{ADR} during training (see \cref{sec:adr_implementation}).

\subsection{Object Set}%
\label{sec:object_set}%

Training uses a set of 13 objects from the VisDex dataset, covering a range of geometries and sizes:
% PLACEHOLDER: Add figure showing all 13 training objects arranged in a grid.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=\textwidth]{training_objects}
%     \caption{The 13 training objects from the VisDex dataset used for multi-object teacher training.}
%     \label{fig:training_objects}
% \end{figure}

The objects include: mario, milk pot, turtle candle holder, toy bagger, teddy bear, closed fist, basketball shoe, chicken head in car, elephant toy, train, homer, toy cow, and plane.
Object meshes are stored as individual \ac{USD} assets.
At episode reset, each parallel environment is assigned one object uniformly at random, and the object's mass, friction, and scale are randomized within the \ac{ADR} ranges.

\subsection{Episode Structure}%
\label{sec:episode_structure}%

Each episode has a maximum duration of 10\,s (600 steps at 60\,Hz) for teacher training and 5\,s (300 steps) for student distillation.
At the start of each episode, the object is placed on a table surface with randomized position and orientation, and the robot is initialized to a fixed home configuration.
The arm is initialized above the object in a top-down approach pose to facilitate contact.

An episode terminates early if any of the following conditions are met:
\begin{itemize}
    \item The object leaves the workspace bounds (\texttt{object\_out\_of\_bound}).
    \item The hand moves too far from the object (\texttt{hand\_too\_far}).
    \item A harmful collision is detected (\texttt{harmful\_collision}).
    \item The palm orientation flips beyond a threshold (\texttt{palm\_flipped}).
\end{itemize}
Early terminations incur a penalty of $-1.0$ on the reward to discourage the policy from exploiting short episodes.

%% =========================================================================
\section{Teacher Policy Implementation}%
\label{sec:teacher_implementation}%

\subsection{Observation Space}%
\label{sec:obs_space}%

The teacher uses an asymmetric actor--critic architecture with different observations for actor and critic, as described in \cref{sec:asymmetric_ac}.
\cref{tab:teacher_obs} details the actor observation vector.

\begin{table}[htb]
    \centering
    \caption{Teacher actor observation space.}
    \label{tab:teacher_obs}
    \begin{tabular}{lcp{6.5cm}}
        \toprule
        \textbf{Component} & \textbf{Dim} & \textbf{Description} \\
        \midrule
        Joint positions & 23 & Arm (7) + hand (16) joint angles \\
        Joint velocities & 23 & Arm (7) + hand (16) joint velocities \\
        Hand position & 3 & Palm center position in world frame \\
        Hand velocity & 3 & Palm center linear velocity \\
        Object position & 3 & Object center of mass position \\
        Object orientation & 4 & Object orientation (quaternion) \\
        Object goal position & 3 & Target lift height \\
        Object scale & 1 & Randomized object scale factor \\
        Previous action & 23 & Action applied at $t-1$ \\
        Teacher one-hot & 1 & Fixed $=1$ (compatibility placeholder) \\
        \midrule
        \textbf{Total} & \textbf{87} & \\
        \bottomrule
    \end{tabular}
\end{table}

The critic observation additionally includes object velocities (6D), contact forces, contact torques, and the teacher one-hot flag, yielding a larger observation vector.
% PLACEHOLDER: Add exact critic observation dimension once confirmed from code.

\subsection{Action Space}%
\label{sec:action_space}%

The action space is 23-dimensional, consisting of joint position targets for all 7 arm joints and 16 hand joints.
Actions are scaled from the policy's normalized output $a_t \in [-1, 1]^{23}$ to absolute joint position targets using per-joint scaling factors.
No geometric fabrics or motion primitives are used; the policy directly commands joint positions, which distinguishes this work from the FGP-based approach in DextrAH-RGB~\cite{Singh2025}.

\subsection{Reward Design}%
\label{sec:reward_implementation}%

The reward function follows the structure defined in \cref{eq:reward}.
\cref{tab:reward_terms} lists all reward terms, their formulations, and weights used in the final training configuration.

\begin{table}[htb]
    \centering
    \caption{Reward terms and weights for teacher training.}
    \label{tab:reward_terms}
    \begin{tabular}{llc}
        \toprule
        \textbf{Term} & \textbf{Formulation} & \textbf{Weight} \\
        \midrule
        Hand-to-object distance & $\exp(-\kappa \cdot \|p_{\text{hand}} - p_{\text{obj}}\|)$ & 4.0 \\
        Palm direction alignment & $\max(0, \hat{n}_{\text{palm}} \cdot \hat{d}_{\text{obj}})$ & 0.5 \\
        Hand-object contact & $\min(n_{\text{contacts}}, n_{\max}) / n_{\max}$ & 8.0 \\
        Good grasp & $\mathbb{1}[\text{contact mask}] \cdot \text{grasp quality}$ & 6.0 \\
        Object-to-goal (lift) & $w_{\text{lift}} \cdot \exp(-\kappa_l \cdot |z_{\text{obj}} - z_{\text{goal}}|) \cdot \mathbb{1}[\text{contact}]$ & 1.0 \\
        Finger curl regularization & $-\|\boldsymbol{q}_{\text{fingers}} - \boldsymbol{q}_{\text{curl}}\|^2$ & 0.5 \\
        Action rate penalty & $-\|a_t - a_{t-1}\|^2$ (fingers $\times 2.5$) & 0.1 \\
        \bottomrule
    \end{tabular}
\end{table}

The reward design underwent extensive iteration.
Key design decisions include:
\begin{itemize}
    \item \textbf{Contact-gated lift reward:} The lift term is multiplied by a contact mask, ensuring the policy only receives lift reward when actually grasping the object, not when the object is accidentally launched upward.
    \item \textbf{Finger action rate penalty scaling:} Hand joints are penalized $2.5\times$ more than arm joints for jerky actions, encouraging smooth finger motions critical for stable grasps.
    \item \textbf{Finger curl regularization:} A weak penalty encourages fingers toward a pre-grasp curl pose, providing an inductive bias for power grasps without constraining the policy.
\end{itemize}

% PLACEHOLDER: Add figure showing reward term evolution during a successful grasp episode (reward breakdown over time).
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.9\textwidth]{reward_breakdown}
%     \caption{Per-step reward decomposition during a representative successful grasp episode.}
%     \label{fig:reward_breakdown}
% \end{figure}

\subsection{Network Architecture}%
\label{sec:teacher_network}%

The teacher policy uses an \ac{LSTM}-based recurrent network implemented in rl\_games.
The actor consists of an MLP encoder followed by an LSTM layer with 1024 hidden units, followed by an output head mapping to the 23-dimensional action space.
The critic has an identical architecture but receives the larger critic observation.

% PLACEHOLDER: Add exact MLP layer sizes (e.g., [512, 256] or [1024, 512]) once confirmed from the YAML config.

\subsection{Training Configuration}%
\label{sec:training_config}%

\cref{tab:ppo_hyperparams} summarizes the \ac{PPO} hyperparameters used for teacher training.

\begin{table}[htb]
    \centering
    \caption{PPO hyperparameters for teacher training.}
    \label{tab:ppo_hyperparams}
    \begin{tabular}{lc}
        \toprule
        \textbf{Hyperparameter} & \textbf{Value} \\
        \midrule
        Learning rate & $1 \times 10^{-4}$ \\
        Clip parameter $\epsilon$ & 0.2 \\
        Mini-epochs per update & 4 \\
        Horizon length & 16 steps \\
        Minibatch size & 2048 \\
        Discount factor $\gamma$ & 0.99 \\
        GAE $\lambda$ & 0.95 \\
        Number of environments & 512 \\
        Max training epochs & 20{,}000 \\
        \bottomrule
    \end{tabular}
\end{table}

Training is performed headless (no rendering) on a single NVIDIA GPU.
% PLACEHOLDER: Specify GPU model (e.g., A100, RTX 4090) and wall-clock training time.

\subsection{Adaptive Domain Randomization}%
\label{sec:adr_implementation}%

The \ac{ADR} curriculum follows the formulation in \cref{sec:adr_formulation} with $N_{\text{max}} = 50$ increments and a success threshold $\tau_{\text{ADR}} = 0.4$.
\cref{tab:adr_params} lists the key randomized parameters and their start/max ranges.

\begin{table}[htb]
    \centering
    \caption{Adaptive Domain Randomization parameters.}
    \label{tab:adr_params}
    \begin{tabular}{lcc}
        \toprule
        \textbf{Parameter} & \textbf{Start Range} & \textbf{Max Range} \\
        \midrule
        Object mass scale & $[1.0, 1.0]$ & $[0.5, 2.0]$ \\
        Object friction (static) & $[1.0, 1.0]$ & $[0.5, 1.5]$ \\
        Robot restitution & $[1.0, 1.0]$ & $[0.8, 1.0]$ \\
        Arm stiffness scale & $[1.0, 1.0]$ & $[0.5, 2.0]$ \\
        Arm damping scale & $[1.0, 1.0]$ & $[0.5, 2.0]$ \\
        Finger MCP pitch stiffness & $[1.0, 1.0]$ & $[0.17, 1.0]$ \\
        Finger MCP pitch damping & $[1.0, 1.0]$ & $[0.083, 1.0]$ \\
        Finger MCP yaw stiffness & $[1.0, 1.0]$ & $[0.025, 1.0]$ \\
        Finger PIP stiffness & $[1.0, 1.0]$ & $[0.02, 1.0]$ \\
        Thumb rotation velocity limit & $[20.0, 20.0]$ & $[0.21, 20.0]$\,rad/s \\
        \bottomrule
    \end{tabular}
\end{table}

The ADR is designed with sim-to-real transfer in mind: the maximum ranges for finger joint stiffness and damping correspond to values measured on the real Agile Hand hardware using an Isaac Lab gain tuner.
The thumb rotation velocity limit is ramped down toward the real hardware limit of approximately 12\,deg/s ($\approx 0.21$\,rad/s).

All ADR parameters share a single increment counter, meaning all parameters progress together.
This simplifies the curriculum but does not allow per-parameter scheduling.

%% =========================================================================
\section{Student Policy Implementation}%
\label{sec:student_implementation}%

\subsection{Student Observation Space}%
\label{sec:student_obs}%

The student replaces the teacher's privileged object state with RGB image observations.
The student observation consists of:
\begin{itemize}
    \item \textbf{Proprioceptive state:} Joint positions (23D), joint velocities (23D), hand position (3D), hand velocity (3D), object goal position (3D), and previous action (23D).
    \item \textbf{Visual input:} Simulated stereo RGB images at $320 \times 240$ resolution from wrist-mounted cameras.
\end{itemize}
Crucially, the student does \emph{not} receive the object position, orientation, scale, or contact information; these must instead be inferred from the visual input.

\subsection{Student Network Architecture}%
\label{sec:student_network}%

The student uses a vision--proprioception fusion architecture:
\begin{enumerate}
    \item \textbf{Visual encoder:} A ResNet-18 backbone processes each stereo image independently, producing a 32-dimensional feature vector per image.
    \item \textbf{Cross-attention fusion:} A cross-attention transformer module fuses the two stereo features into a single 64-dimensional visual embedding.
    \item \textbf{Policy head:} The visual embedding is concatenated with the proprioceptive state vector and fed into an \ac{LSTM} with 512 hidden units, followed by a linear output layer producing the 23-dimensional action.
\end{enumerate}

% PLACEHOLDER: Add figure showing the student network architecture (visual encoder -> cross-attention -> LSTM -> action).
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.85\textwidth]{student_architecture}
%     \caption{Student policy network architecture. Stereo RGB images are encoded by a shared ResNet-18 backbone, fused via cross-attention, and concatenated with proprioceptive features before an LSTM policy head.}
%     \label{fig:student_architecture}
% \end{figure}

\subsection{SafeDagger Distillation Configuration}%
\label{sec:distillation_config}%

The distillation follows the SafeDAgger procedure described in \cref{alg:safedagger}.
The safety policy uses the per-environment L2 action error as the intervention criterion: for each environment, the teacher takes over when $\|e_t\|_2 > \delta$, with $\delta = 0.5$.
The intervention rate $\beta_{\text{safe}}$ (\cref{eq:beta_safedagger}) is computed as the fraction of environments where the threshold is exceeded.

Key distillation hyperparameters:
\begin{itemize}
    \item \textbf{Optimizer:} Adam, learning rate $1 \times 10^{-4}$
    \item \textbf{Distillation iterations:} 350{,}000 (default)
    \item \textbf{Number of environments:} 16 (with cameras enabled)
    \item \textbf{ADR:} Disabled during distillation (\texttt{enable\_adr=False})
    \item \textbf{Arm randomization:} Disabled (\texttt{disable\_arm\_randomization=True})
    \item \textbf{Stereo rendering:} Enabled (\texttt{simulate\_stereo=True})
\end{itemize}

% PLACEHOLDER: Add wall-clock time for distillation (first camera run includes 10-20 min shader compilation).

%% =========================================================================
\section{Evaluation Protocol}%
\label{sec:eval_protocol}%

\subsection{Metrics}%
\label{sec:metrics}%

The following metrics are used to evaluate both teacher and student policies:

\begin{description}
    \item[\ac{GSR}:] Fraction of episodes where the object is lifted above the table surface and held with stable contact for at least 0.5\,s (30 steps at 60\,Hz). The hold requirement prevents counting accidental upward launches as successful grasps.
    \item[\ac{UER}:] Fraction of episodes containing at least one early termination due to an unsafe event. Unsafe events are classified into five categories: \texttt{object\_out\_of\_bound}, \texttt{hand\_too\_far}, \texttt{harmful\_collision}, \texttt{palm\_flipped}, and \texttt{physics\_instability} (simulation-only artifacts such as NaN joint states or velocity explosions, not relevant for real-world deployment).
    \item[\ac{TQR}:] For SafeDAgger, the fraction of time steps where the teacher provides the action (i.e., $D_\psi(o_t) < 0.5$). This quantifies the student's autonomy during distillation.
    \item[Imitation loss:] The L2 action error between student and teacher, averaged over the evaluation batch.
\end{description}

\subsection{Evaluation Procedure}%
\label{sec:eval_procedure}%

Teacher policies are evaluated using \texttt{eval\_teacher.py}, which supports both single-checkpoint and multi-teacher (per-object directory) evaluation.
Student policies are evaluated using \texttt{eval\_student.py}, which additionally reports per-object metrics, failure reason breakdown, and the hold-gated lift success criterion.
% PLACEHOLDER: Specify number of evaluation episodes per checkpoint (e.g., 100 episodes per object, 3 seeds).

%% =========================================================================
\section{Teacher Training Results}%
\label{sec:teacher_results}%

\subsection{Training Progression}%
\label{sec:training_progression}%

The teacher policy was trained on 13 objects from the VisDex dataset using 512 parallel environments.
Training proceeded in two phases:
\begin{enumerate}
    \item \textbf{Initial training (16 envs, runs 1a--1o):} A series of iterative experiments to resolve physics stability issues, reward shaping, and early termination handling. With only 16~environments across 13~objects ($\approx$1.2~envs per object), training exhibited high gradient variance and cyclic performance collapses.
    \item \textbf{Scaled training (512 envs, run~1p):} Resumed from the best checkpoint of the initial phase with 512~environments ($\approx$39~envs per object), yielding stable gradient estimates and consistent learning progress.
\end{enumerate}

% PLACEHOLDER: Add teacher training curve figure (reward and ADR level vs. epoch).
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.85\textwidth]{teacher_training_curve}
%     \caption{Teacher policy training: mean episode reward (blue, left axis) and ADR level (orange, right axis) over training epochs. Training was resumed at epoch~$\sim$8000 with 512 environments. The first ADR increment occurred at epoch~8850.}
%     \label{fig:teacher_training_curve}
% \end{figure}

The policy reached ADR level~5 by epoch~11{,}124, demonstrating reliable grasping and lifting across all 13 objects.
A subsequent extended run with 1024 environments reached ADR level~19 after 20{,}000 epochs, producing a stronger teacher for distillation.

\subsection{Physics Stability Challenges}%
\label{sec:physics_challenges}%

A central challenge in training was achieving stable contact physics for the Agile Hand.
The key failure modes and their resolutions are summarized in \cref{tab:physics_challenges}.

\begin{table}[htb]
    \centering
    \caption{Physics stability challenges encountered during teacher training.}
    \label{tab:physics_challenges}
    \begin{tabular}{p{3cm}p{4.5cm}p{4.5cm}}
        \toprule
        \textbf{Failure Mode} & \textbf{Root Cause} & \textbf{Resolution} \\
        \midrule
        Velocity explosion on every reset & Joint init position outside joint limits (thumb\_rot) & Corrected init to $-20\degree$ (joint minimum) \\
        Finger joints explode on contact & Effort limit 10\,Nm (10$\times$ too high for hand joints) & Reduced to 2.0\,Nm \\
        Policy avoids contact & Contact causes simulation instability $\to$ early termination $\to$ negative reward & Physics stability overhaul: damping $3 \to 6$, effort limits reduced \\
        Gradient oscillation & 16 envs / 13 objects $\to$ $\sim$1 env per object & Scaled to 512 environments \\
        Reward hacking (hover) & Dense rewards achievable without contact & Contact-gated lift reward, early termination penalty \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Teacher Performance}%
\label{sec:teacher_performance}%

\cref{tab:teacher_performance} summarizes the performance of the two teacher checkpoints used for distillation experiments.

\begin{table}[htb]
    \centering
    \caption{Teacher policy performance (multi-object, 13 VisDex objects). The ADR-19 teacher was evaluated over 80 episodes (16 envs $\times$ 5 rollouts) at ADR-0 physics.}
    \label{tab:teacher_performance}
    \begin{tabular}{lcc}
        \toprule
        \textbf{Metric} & \textbf{ADR-5 Teacher} & \textbf{ADR-19 Teacher} \\
        \midrule
        ADR level reached & 5 & 19 \\
        Training epochs & 11{,}124 & 20{,}000 \\
        Number of environments & 512 & 1{,}024 \\
        Mean episode reward & 12{,}104 & 5{,}317 \\
        \acs{GSR} (\%) & -- & 96.25 \\
        \acs{UER} (\%) & -- & 53.75 \\
        \bottomrule
    \end{tabular}
\end{table}

Note that the ADR-19 teacher has a lower mean reward than the ADR-5 teacher because it operates under significantly more challenging physics conditions (wider randomization ranges).
The lower reward reflects harder conditions, not worse policy quality.
The teacher's 53.75\% \ac{UER} is dominated by \texttt{physics\_instability} (55.8\% of all failures), which are simulation-only artifacts (NaN joint states, velocity explosions) that cannot occur on real hardware.
Excluding these, the real-world-relevant unsafe rate is approximately 24\%, with the remaining failures split between \texttt{object\_out\_of\_bound} (32.6\%) and \texttt{harmful\_collision} (11.6\%).

% PLACEHOLDER: Add qualitative figure showing grasp sequences on 3-4 different objects.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=\textwidth]{grasp_sequences}
%     \caption{Representative grasp sequences from the ADR-19 teacher on four objects. Top row: approach phase. Bottom row: stable grasp and lift.}
%     \label{fig:grasp_sequences}
% \end{figure}

%% =========================================================================
\section{Distillation Results}%
\label{sec:distillation_results}%

Three SafeDAgger distillation runs were performed, progressively increasing teacher strength and training duration.
All use the 13-object VisDex set and identical student architectures.

\subsection{SafeDAgger Distillation}%
\label{sec:safedagger_results}%

\cref{tab:distillation_results} compares the three distillation runs.
Runs~1a and 1b used 100k iterations with 8~environments; run~1c extended to 350k iterations with 16~environments.

\begin{table}[htb]
    \centering
    \caption{SafeDAgger distillation results (during-training metrics, including teacher-assisted actions).}
    \label{tab:distillation_results}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Metric} & \textbf{Run~1a} & \textbf{Run~1b} & \textbf{Run~1c} \\
        & \textbf{(ADR-5, 100k)} & \textbf{(ADR-19, 100k)} & \textbf{(ADR-19, 350k)} \\
        \midrule
        Imitation loss (L2) & 2.10 & 1.48 & 1.40 \\
        Lifted (\%) & 62.5 & 87.5 & 62.5 \\
        In goal (\%) & 37.5 & 62.5 & 37.5 \\
        $\beta_{\text{safe}}$ & 1.0 & 0.875 & 0.75 \\
        Num.\ environments & 8 & 8 & 16 \\
        \bottomrule
    \end{tabular}
\end{table}

\paragraph{Effect of teacher strength.}
Comparing runs~1a and 1b (same iterations, different teachers), the stronger ADR-19 teacher yields a 40\% relative improvement in lift rate (62.5\% $\to$ 87.5\%), a 66\% improvement in goal rate (37.5\% $\to$ 62.5\%), and a 30\% reduction in imitation loss (2.10 $\to$ 1.48).

\paragraph{Effect of training duration.}
Extending from 100k to 350k iterations (run~1c) further reduced the imitation loss (1.48 $\to$ 1.40) and the intervention rate (0.875 $\to$ 0.75), indicating continued learning.
However, the during-training metrics are misleading because they include teacher-assisted actions, as the teacher still performs the grasping in 75\% of environments.

\paragraph{Standalone student evaluation.}
To measure true student performance without teacher assistance, the run~1c checkpoint was evaluated using \texttt{eval\_student.py} over 80~episodes (16~envs $\times$ 5~rollouts).
\cref{tab:standalone_eval} compares the standalone student against the teacher ceiling.

\begin{table}[htb]
    \centering
    \caption{Standalone student vs.\ teacher performance (80 episodes, ADR-0 physics).}
    \label{tab:standalone_eval}
    \begin{tabular}{lcc}
        \toprule
        \textbf{Metric} & \textbf{Teacher (ADR-19)} & \textbf{Student (run~1c)} \\
        \midrule
        \acs{GSR} (\%) & 96.25 & 36.25 \\
        \acs{UER} (\%) & 53.75 & 67.50 \\
        \multicolumn{3}{l}{\emph{Failure breakdown (\% of unsafe episodes):}} \\
        \quad Physics instability & 55.8 & -- \\
        \quad Harmful collision & 11.6 & 40.7 \\
        \quad Object out of bound & 32.6 & 33.3 \\
        \quad Palm flipped & 0.0 & 1.9 \\
        \quad Hand too far & 0.0 & 0.0 \\
        \bottomrule
    \end{tabular}
\end{table}

The student achieves 36.25\% standalone lift success, which represents a significant gap from the 96.25\% teacher ceiling.
The primary failure mode is \texttt{harmful\_collision} (40.7\% of failures vs.\ 11.6\% for the teacher), indicating the student crashes into the table surface 3.5$\times$ more frequently.
Object knockoff rates are comparable ($\sim$33\%), suggesting this is an inherent task difficulty rather than a student-specific weakness.
Arm control is robust for both teacher and student (no \texttt{hand\_too\_far} failures).

% PLACEHOLDER: Add distillation learning curve figure (imitation loss, lift rate, beta over iterations).
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.85\textwidth]{distillation_curves}
%     \caption{SafeDAgger distillation learning curves. Left: imitation loss. Center: lift success rate. Right: teacher intervention rate $\beta_{\text{safe}}$. Blue: ADR-5 teacher. Orange: ADR-19 teacher.}
%     \label{fig:distillation_curves}
% \end{figure}

\subsection{DAgger Baseline Comparison}%
\label{sec:dagger_results}%

A central goal of this work is a controlled comparison between vanilla DAgger and SafeDAgger for distilling a privileged dexterous grasping teacher into a vision-based student policy.
The original SafeDAgger paper~\cite{Zhang2016} evaluated only in simulated autonomous driving (TORCS) and compared solely against vanilla DAgger, without systematic ablation of the safety threshold or application to contact-rich manipulation.
The DexSafeDagger extension~\cite{ChiZhang2026} adds Q-value-based risk gating, but no published comparison exists on the specific platform and object set used here.
This section addresses both gaps by providing a head-to-head evaluation under matched conditions.

\paragraph{Experimental design.}
Both methods use the same ADR-19 teacher, 13-object VisDex set, identical student architecture (ResNet-18 + cross-attention + LSTM-512), 16 parallel environments, and 350k distillation iterations.
The only difference is the intervention mechanism:
\begin{itemize}
    \item \textbf{DAgger:} The student always executes its own actions.  The teacher provides target action labels at every time step, and the student is trained with KL divergence loss (following the DextrAH-RGB finding that KL consistently outperforms L2 for action distillation~\cite{Singh2025}).
    \item \textbf{SafeDAgger:} The teacher takes over execution in environments where $\|e_t\|_2 > \delta$ (with $\delta = 0.5$), shielding the student from high-error states. The student is trained with L2 loss on all observed state--action pairs.
\end{itemize}
This design isolates the effect of SafeDAgger's selective intervention: does shielding the student from dangerous states improve safety at the cost of task performance, or does it also accelerate learning by providing a form of automated curriculum~\cite{Zhang2016}?

% PLACEHOLDER: DAgger baseline (run2a) is in progress with 350k iterations, 16 envs, ADR-19 teacher. Fill in results when complete.

\begin{table}[htb]
    \centering
    \caption{Head-to-head comparison of DAgger and SafeDAgger distillation (ADR-19 teacher, 13 objects, 350k iterations, 80 standalone evaluation episodes each).}
    \label{tab:dagger_comparison}
    \begin{tabular}{lcc}
        \toprule
        \textbf{Metric} & \textbf{DAgger (KL loss)} & \textbf{SafeDAgger (L2 loss)} \\
        \midrule
        Final \acs{GSR} (\%) & -- & 36.25 \\
        \acs{UER} (\%) & -- & 67.50 \\
        \acs{TQR} (\%) & 100 (by design) & 75.0 \\
        Imitation loss & -- & 1.40 (L2) \\
        Harmful collisions (\% of failures) & -- & 40.7 \\
        Object out of bound (\% of failures) & -- & 33.3 \\
        Context switches (per episode) & 0 & -- \\
        \bottomrule
    \end{tabular}
\end{table}

The comparison addresses three specific hypotheses:
\begin{enumerate}
    \item \textbf{Safety hypothesis:} SafeDAgger produces a student with fewer unsafe episodes (lower \ac{UER}) during both training and standalone evaluation, particularly fewer harmful collisions.
    \item \textbf{Performance hypothesis:} SafeDAgger's intervention mechanism may reduce standalone \ac{GSR} by preventing the student from practicing in challenging states where it needs the most experience.
    \item \textbf{Efficiency hypothesis:} SafeDAgger requires fewer total teacher queries (lower \ac{TQR}) to reach comparable performance, as it selectively queries only on difficult states, creating an ``automated curriculum'' effect observed in the original SafeDAgger paper~\cite{Zhang2016}.
\end{enumerate}

% PLACEHOLDER: Fill in DAgger column of the table and add the following analysis once run2a completes:
% - Compare GSR: does DAgger achieve higher standalone grasp success?
% - Compare UER: does SafeDAgger produce fewer harmful collisions?
% - Compare learning dynamics: does DAgger converge faster or slower?
% - Analyze whether the KL vs L2 loss difference confounds the comparison.

% PLACEHOLDER: Add DAgger vs. SafeDAgger comparison figure once DAgger run completes.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.85\textwidth]{dagger_vs_safedagger}
%     \caption{DAgger vs.\ SafeDAgger: grasp success rate (left) and unsafe episode rate (right) over distillation iterations.}
%     \label{fig:dagger_vs_safedagger}
% \end{figure}

\subsection{Safety Analysis}%
\label{sec:safety_analysis}%

The standalone student evaluation reveals that the dominant failure mode is \texttt{harmful\_collision} (40.7\% of unsafe episodes), where the hand or arm contacts the table surface.
This is 3.5$\times$ higher than the teacher (11.6\%), indicating that the student has not fully learned the teacher's collision-avoidance behavior from visual input alone.
Object knockoff (\texttt{object\_out\_of\_bound}, 33.3\%) is comparable between teacher and student, suggesting this is inherent to the grasp difficulty rather than a perception failure.
Notably, no \texttt{hand\_too\_far} failures occur for either teacher or student, indicating that the arm positioning component of the policy transfers well through distillation.

% PLACEHOLDER: Add safety breakdown comparison figure once DAgger baseline completes.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.8\textwidth]{safety_breakdown}
%     \caption{Breakdown of unsafe episode types for teacher, SafeDAgger student, and DAgger student.}
%     \label{fig:safety_breakdown}
% \end{figure}

\subsection{Intervention Analysis}%
\label{sec:intervention_analysis}%

The SafeDAgger intervention rate $\beta_{\text{safe}}$ provides insight into the student's learning progress.
Over the course of training, $\beta_{\text{safe}}$ decreased from 1.0 (100\% teacher control) at 100k iterations to 0.75 at 350k iterations, indicating that the student progressively takes over more environments.
However, at $\beta_{\text{safe}} = 0.75$ the teacher still controls 75\% of environments, contributing to the gap between during-training metrics (62.5\% lifted) and standalone evaluation (36.25\% \ac{GSR}).
This gap highlights a key limitation of SafeDAgger's intervention mechanism: by shielding the student from difficult states, the teacher prevents the student from practicing precisely the situations where it needs the most experience.

% PLACEHOLDER: Add intervention rate decay curve over training iterations.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.7\textwidth]{intervention_rate}
%     \caption{Teacher intervention rate $\beta_{\text{safe}}$ over SafeDAgger distillation iterations.}
%     \label{fig:intervention_rate}
% \end{figure}

%% =========================================================================
\section{Ablation Studies}%
\label{sec:ablations}%

\subsection{Effect of Number of Training Environments}%
\label{sec:ablation_envs}%

A critical finding from the teacher training experiments is the importance of sufficient parallel environments for multi-object training.
With 16~environments across 13~objects, the policy exhibited cyclic performance collapses due to high gradient variance ($\sim$1.2~envs per object per batch).
Scaling to 512~environments ($\sim$39~envs per object) produced stable, monotonic learning progress and enabled the first successful ADR progression.
This suggests a minimum of $\sim$30 environments per object class is necessary for stable multi-object policy training.

\subsection{Effect of Early Termination Penalty}%
\label{sec:ablation_penalty}%

Without an early termination penalty, the teacher policy learned to exploit short episodes: minimizing total negative reward by terminating quickly rather than attempting to grasp.
Introducing a flat penalty of $-1.0$ per early termination eliminated this failure mode and was necessary for the policy to learn approach and contact behaviors.
A higher penalty of $-3.0$ was initially tested but was reduced because it caused the policy to avoid any behavior that risked termination, including the necessary approach phase.

\subsection{Effect of Teacher Strength on Distillation}%
\label{sec:ablation_teacher_strength}%

The comparison between ADR-5 and ADR-19 teachers (\cref{tab:distillation_results}) demonstrates that teacher policy quality is a primary bottleneck for distillation performance.
The stronger teacher produces more consistent demonstrations, leading to lower imitation loss and higher task success.
This aligns with the general finding in the imitation learning literature that demonstration quality matters more than demonstration quantity.

\subsection{LSTM Checkpoint Resume for ADR Curriculum}%
\label{sec:ablation_lstm_resume}%

An important negative finding from the sim-to-real curriculum experiments (exp\_03) is that \textbf{resuming LSTM-based teacher policies from checkpoints does not work reliably for continued ADR training}.
Three independent runs (1a--1c) resumed from the ADR-19 checkpoint and all degraded at ADR levels 16--18, with lift success dropping from $\sim$70\% to $\sim$15--40\%.

Investigation of the rl\_games checkpoint format confirmed the root cause: while model weights, optimizer state, and running statistics are saved, \textbf{LSTM hidden states are reset to zero on load}.
The LSTM must rebuild its temporal representations from scratch.
At low ADR levels this is feasible (the task is easy enough), but at high ADR levels the difficulty margin is too thin for a cold-started LSTM to re-learn temporal context while also handling physics randomization.
The original training succeeded because the LSTM built context \emph{continuously} through ADR levels 0$\to$19.

This finding motivated training the sim-to-real teacher from scratch (run~1d) rather than fine-tuning the existing checkpoint, incorporating all sim-to-real curriculum terms from the beginning.

\subsection{Safety Threshold Sensitivity}%
\label{sec:ablation_threshold}%

A notable gap in the original SafeDAgger paper~\cite{Zhang2016} is the lack of a systematic sensitivity analysis for the safety threshold $\delta$.
Zhang and Cho selected a single threshold ($\tau = 0.0025$ in their steering-angle domain) based on the error distribution of the initial supervised policy and did not vary it.
This work addresses that gap by sweeping $\delta$ across five values: $\{0.1, 0.3, 0.5, 0.7, 1.0\}$.

For each threshold, a SafeDAgger distillation run is performed with the ADR-19 teacher, 16~environments, and 350k iterations.
Lower $\delta$ values trigger teacher intervention more aggressively (more protective, fewer student-executed actions), while higher values allow the student to act autonomously in more states (less protective, more exploration).
The extreme case $\delta \to \infty$ recovers vanilla DAgger (no intervention).

% PLACEHOLDER: Run the five threshold experiments and fill in the table below.

\begin{table}[htb]
    \centering
    \caption{Effect of safety threshold $\delta$ on SafeDAgger distillation performance (ADR-19 teacher, 350k iterations, standalone evaluation over 80 episodes).}
    \label{tab:threshold_sweep}
    \begin{tabular}{lcccc}
        \toprule
        $\delta$ & \acs{GSR} (\%) & \acs{UER} (\%) & \acs{TQR} (\%) & Harmful collisions (\%) \\
        \midrule
        0.1 & -- & -- & -- & -- \\
        0.3 & -- & -- & -- & -- \\
        0.5 & 36.25 & 67.50 & 75.0 & 40.7 \\
        0.7 & -- & -- & -- & -- \\
        1.0 & -- & -- & -- & -- \\
        $\infty$ (DAgger) & -- & -- & 100 & -- \\
        \bottomrule
    \end{tabular}
\end{table}

This ablation reveals the safety--performance Pareto frontier for the dexterous grasping domain.
If low-$\delta$ runs show lower \ac{UER} but also lower \ac{GSR}, the threshold acts as an explicit knob for trading safety against task performance, which is a critical design parameter for eventual real-world deployment where hardware damage is costly.
Conversely, if an intermediate $\delta$ simultaneously improves both metrics, it would confirm the ``automated curriculum'' hypothesis from Zhang and Cho: that focusing training on difficult states accelerates learning without sacrificing safety.

% PLACEHOLDER: Add Pareto curve figure (x-axis: TQR or UER, y-axis: GSR) showing each threshold as a point.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.7\textwidth]{threshold_pareto}
%     \caption{Safety--performance Pareto frontier: standalone \ac{GSR} vs.\ \ac{UER} for varying SafeDAgger thresholds $\delta$. The DAgger baseline ($\delta = \infty$) is shown for reference.}
%     \label{fig:threshold_pareto}
% \end{figure}

\subsection{Per-Object Performance Breakdown}%
\label{sec:ablation_per_object}%

The 13 VisDex training objects span a wide range of geometries, from compact, graspable shapes such as the basketball shoe and teddy bear, to thin or irregular objects such as the plane and toy bagger.
To identify which object categories benefit most from SafeDAgger's intervention mechanism, both the teacher and student policies are evaluated per-object over 80~episodes each (16~envs $\times$ 5~rollouts, with each environment assigned a single object).

% PLACEHOLDER: Run per-object evaluations for teacher, SafeDAgger student, and DAgger student. Fill in the table below.

\begin{table}[htb]
    \centering
    \caption{Per-object \ac{GSR} (\%) for teacher, SafeDAgger student, and DAgger student (80 evaluation episodes per object, ADR-0 physics).}
    \label{tab:per_object_gsr}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Object} & \textbf{Teacher} & \textbf{SafeDAgger} & \textbf{DAgger} \\
        \midrule
        Mario          & -- & -- & -- \\
        Milk pot       & -- & -- & -- \\
        Turtle candle  & -- & -- & -- \\
        Toy bagger     & -- & -- & -- \\
        Teddy bear     & -- & -- & -- \\
        Closed fist    & -- & -- & -- \\
        Basketball shoe & -- & -- & -- \\
        Chicken in car & -- & -- & -- \\
        Elephant toy   & -- & -- & -- \\
        Train          & -- & -- & -- \\
        Homer          & -- & -- & -- \\
        Toy cow        & -- & -- & -- \\
        Plane          & -- & -- & -- \\
        \midrule
        \textbf{Mean}  & 96.25 & 36.25 & -- \\
        \bottomrule
    \end{tabular}
\end{table}

The per-object breakdown serves two purposes.
First, it identifies which object geometries pose the greatest challenge for the vision-based student, informing whether the teacher--student gap is uniform or concentrated on specific shapes.
Second, it reveals whether SafeDAgger's intervention is more beneficial for certain object categories, for example thin or irregular objects where collisions are more likely and teacher shielding may be more valuable.

% PLACEHOLDER: Add bar chart comparing per-object GSR for teacher, SafeDAgger, and DAgger.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=\textwidth]{per_object_comparison}
%     \caption{Per-object \ac{GSR} comparison. Blue: teacher. Orange: SafeDAgger student. Green: DAgger student. Objects sorted by teacher \ac{GSR} (descending).}
%     \label{fig:per_object_comparison}
% \end{figure}

\subsection{Query Efficiency Analysis}%
\label{sec:ablation_query_efficiency}%

A central claim of SafeDAgger is \emph{query efficiency}: by selectively querying the teacher only on states where the student struggles, SafeDAgger should achieve comparable performance with fewer total teacher queries than vanilla DAgger~\cite{Zhang2016}.
In the original driving domain, SafeDAgger collected 70k targeted examples compared to DAgger's 390k uniformly sampled examples per iteration.

In the dexterous grasping setting, query efficiency is measured by the cumulative number of teacher-executed time steps over the course of distillation.
For DAgger, this equals the total number of time steps (the teacher always provides labels, though the student always acts).
For SafeDAgger, this equals $\text{TQR} \times \text{total steps}$, since the teacher only acts in environments where $\|e_t\|_2 > \delta$.

% PLACEHOLDER: Compute and plot the following once DAgger baseline completes:
% - Cumulative teacher queries vs. distillation iteration for both methods
% - GSR vs. cumulative teacher queries (query efficiency curve)
% The key question: does SafeDAgger reach a given GSR with fewer teacher queries?

% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.85\textwidth]{query_efficiency}
%     \caption{Query efficiency comparison. Left: cumulative teacher queries over distillation iterations. Right: standalone \ac{GSR} vs.\ cumulative teacher queries. SafeDAgger's selective querying is expected to achieve higher GSR per teacher query.}
%     \label{fig:query_efficiency}
% \end{figure}

\subsection{Learning Curve Comparison}%
\label{sec:ablation_learning_curves}%

To compare convergence dynamics, \ac{GSR} and \ac{UER} are evaluated at regular checkpoints throughout distillation for both DAgger and SafeDAgger.
Checkpoints are saved every 50k iterations, yielding seven evaluation points per method (at 0k, 50k, 100k, 150k, 200k, 250k, 300k, and 350k iterations).
Each checkpoint is evaluated standalone (without teacher intervention) over 80~episodes.

% PLACEHOLDER: Run checkpoint evaluations for both methods and plot learning curves.
% \begin{figure}[htb]
%     \centering
%     \includegraphics[width=0.85\textwidth]{learning_curves}
%     \caption{Learning curve comparison over distillation iterations. Left: standalone \ac{GSR}. Right: \ac{UER}. Solid: SafeDAgger. Dashed: DAgger. Shaded regions indicate $\pm 1$ standard deviation across evaluation seeds.}
%     \label{fig:learning_curves}
% \end{figure}

The learning curves address whether SafeDAgger's curriculum effect, which focuses training on difficult states, leads to faster initial convergence or whether DAgger's unrestricted exploration enables the student to eventually surpass SafeDAgger given sufficient iterations.
The \ac{UER} curves additionally reveal whether SafeDAgger students exhibit fewer unsafe behaviors throughout training, not just at the final checkpoint.

\subsection{Effect of Distillation Duration}%
\label{sec:ablation_duration}%

The comparison between runs~1b (100k iterations) and 1c (350k iterations) in \cref{tab:distillation_results} suggests continued learning beyond 100k iterations.
To determine whether the student has converged or would benefit from extended training, the learning curve analysis above is complemented by additional long-horizon runs at 500k and 750k iterations.
If the \ac{GSR} curve plateaus, the student has reached its capacity given the current architecture; if it continues to rise, the 350k baseline underestimates the method's potential.

% PLACEHOLDER: Run extended distillation (500k, 750k iterations) for both DAgger and SafeDAgger. Report final GSR, UER, and imitation loss.

%% =========================================================================
\section{Sim-to-Real Curriculum}%
\label{sec:sim2real_curriculum}%

To close the sim-to-real gap, a second-generation teacher is being trained from scratch with hardware-realistic constraints incorporated into the \ac{ADR} curriculum.

\subsection{Curriculum Design}%
\label{sec:sim2real_design}%

The sim-to-real curriculum adds three categories of hardware constraints that are progressively introduced via \ac{ADR}:
\begin{enumerate}
    \item \textbf{Finger actuator gains:} Per-joint-group stiffness and damping scales, starting at nominal simulation values and widening toward hardware measurements. Initial attempts with hardware-exact gain targets (e.g., MCP pitch stiffness scale down to 0.17) caused policy collapse; relaxed ranges of $[0.1, 1.0]$ proved stable.
    \item \textbf{Effort and velocity limits:} The thumb rotation velocity limit is ramped from 20\,rad/s (simulation) toward 0.21\,rad/s ($\approx$12\,deg/s, hardware limit). Arm effort limits are reduced toward FR3 factory specs (87\,Nm for joints~1--4, 12\,Nm for joints~5--7).
    \item \textbf{Reward rebalancing:} The object-to-goal weight was doubled ($20 \to 40$) and a success bonus ($w = 10$) was added, since the original reward balance caused the policy to optimize for holding objects rather than delivering them to the goal height.
\end{enumerate}

\subsection{Preliminary Results}%
\label{sec:sim2real_results}%

Training from scratch with the full sim-to-real curriculum (run~1d, 1024 envs) reached ADR level~12 of 50 after 45{,}000 epochs.
The policy achieves $\sim$54\% lift success but only $\sim$30\% goal delivery, limited by the reward imbalance identified above.
A follow-up run (1e) with rebalanced rewards and multi-GPU training (2048 envs across 2 GPUs) is in progress.

% PLACEHOLDER: Add sim2real training curve figure and final results when run1e completes.

%% =========================================================================
\section{Summary of Findings}%
\label{sec:results_summary}%

The key experimental findings are:

\begin{enumerate}
    \item \textbf{Physics stability is the primary challenge} for training dexterous grasping policies with custom hand assets in Isaac Lab.  Actuator parameters (effort limits, damping) must be carefully tuned to prevent simulation instabilities at contact, which otherwise cause the policy to learn contact-avoidance behaviors.
    \item \textbf{Sufficient parallel environments are critical} for multi-object teacher training.  A minimum of $\sim$30 environments per object class is needed to avoid gradient oscillation and cyclic collapses.
    \item \textbf{Teacher policy quality is the primary bottleneck} for distillation.  The ADR-19 teacher achieves 96.25\% \ac{GSR}, providing a high performance ceiling.  Stronger teachers produce better students (40\% relative improvement from ADR-5 to ADR-19).
    \item \textbf{SafeDAgger produces functional but limited students:} 36.25\% standalone \ac{GSR} after 350k iterations, compared to the 96.25\% teacher ceiling.  The primary gap is in collision avoidance (3.5$\times$ more harmful collisions than the teacher).
    \item \textbf{SafeDAgger's intervention mechanism is a double-edged sword:} It prevents unsafe student actions during training but also limits the student's exposure to challenging states, potentially slowing learning.  The DAgger baseline comparison (\cref{sec:dagger_results}) and safety threshold sweep (\cref{sec:ablation_threshold}) are designed to quantify this trade-off and reveal the safety--performance Pareto frontier.
    \item \textbf{LSTM policies cannot be fine-tuned from checkpoints} for continued ADR curriculum training, due to hidden state reset.  Sim-to-real teachers must be trained from scratch with all curriculum terms included from the start.
\end{enumerate}

The ablation studies additionally investigate:
\begin{itemize}
    \item The safety threshold $\delta$ as an explicit knob for trading safety against performance (\cref{sec:ablation_threshold}), addressing a gap in the original SafeDAgger work~\cite{Zhang2016}.
    \item Per-object performance variation (\cref{sec:ablation_per_object}), revealing which geometries benefit most from teacher intervention during distillation.
    \item Query efficiency (\cref{sec:ablation_query_efficiency}), testing whether SafeDAgger's selective querying achieves comparable \ac{GSR} with fewer teacher interactions.
    \item Learning dynamics (\cref{sec:ablation_learning_curves}), comparing convergence speed and safety profiles throughout training, not just at the final checkpoint.
\end{itemize}

% PLACEHOLDER: Update this summary with concrete findings once all experiments complete (DAgger baseline, threshold sweep, per-object eval, query efficiency, learning curves, extended duration runs).
%
